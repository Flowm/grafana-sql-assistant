import { CSSProperties } from 'react';
import { flexSpaceBetween } from '../../styles/common.styles';

export const chatHeaderStyles: CSSProperties = {
  ...flexSpaceBetween,
  margin: 0,
};

export const chatHeaderTitleStyles: CSSProperties = {
  margin: 0,
};
